<%- include ('../playout/header.ejs') -%>
<div class="container">
    <% if(msg != '') {%>
        <div class="alert alert-success row"><%= msg%></div>
    <%};%>    
    <% if(errors) { %>
    
        <% errors.forEach(function(e){ %>
    <div class="alert alert-danger row ">
        <%= e%>    
    </div>
    <%});%>
    <% };%>
    <div class="row">
        <form action="/cates/addcate" method="POST">
            <div class="form-group">
                <label for="category">Lo&#x1EA1;i h&agrave;ng</label>
                <input id="category" class="form-control" type="text" placeholder="Nhập loại mặt hàng" name="name"/>
            </div>
            <button class="btn btn-primary" type="submit">Submit</button>
        </form>
    </div> 
    
</div>
<%- include ('../playout/footer.ejs') -%>